export enum QueryParamsEnums {
  people = 'people',
  planets = 'planets',
  species = 'species',
  films = 'films',
  starships = 'starships',
  vehicles = 'vehicles',
}

export enum Urls {
  baseUrl = 'https://swapi.dev/api/',
}
